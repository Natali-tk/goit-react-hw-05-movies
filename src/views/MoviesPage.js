export default function MoviesPage() {
  //    const[movies, setMovies]=useState([]);
  //    useEffect(() => {
  //     fetchTrandingMovies().then(setMovies);
  //    }, [])

  return (
    <>
      <h2>вы можете найти фильм по названию</h2>
      <form>
        <input></input>
      </form>
      {/* <ul >
              {movies.map(movie=>(
                  <li >
                      {movie.name}
                  </li>
              ))}
    
          </ul> */}
    </>
  );
}
